/// <reference types="vite/client" />

/** Injected at build time by vite.config.ts — the commit the bundle was built from. */
declare const __BUILD_ID__: string;
declare const __BUILD_TIME__: string;
